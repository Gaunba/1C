Строка = "";
Темп = "";

ВвестиСтроку(Строка);

Массив1 = СтрРазделить(Строка, " ");

Итер = Массив1.Количество() - 1;

Пока Итер >= 0 Цикл
     Темп = Темп + Массив1[Итер] + " ";
	 Итер = Итер - 1;
	КонецЦикла;

Темп = ВРег(Темп);
ОткрытьЗначение(Темп);
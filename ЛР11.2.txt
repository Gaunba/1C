Строка = "";
Символ = "";
Числ = 0;
ВвестиСтроку(Строка);
Для Переменная = 1 по СтрДлина(Строка) Цикл
	Если Числ < КодСимвола(Сред(Строка, Переменная, 1)) Тогда
		Символ = Сред(Строка, Переменная, 1);
		Числ = КодСимвола(Сред(Строка, Переменная, 1));
		КонецЕсли;
	КонецЦикла;
	
ОткрытьЗначение(Символ);